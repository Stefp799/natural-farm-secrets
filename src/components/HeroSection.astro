---
// HeroSection.astro
---

<section class="hero">
  <header class="nav">
    <div class="logo">
    <img src="/logonoback250.png" alt="Natural Farm Secrets" />
    Natural Farm Secrets
    </div>
    <nav class="menu">
      <a href="#teachers">Teachers</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <video autoplay muted loop playsinline class="bg-video">
    <source src="/WheatField.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
  <div class="bg-red-500 text-white p-4 text-xl mt-6 rounded">
      Tailwind is working
    </div>
  <div class="hero-text">
    <h1>Farming Wisdom, AI-Powered</h1>
    <h2>Learn from avatars, rooted in soil and science</h2>
  </div>

 <style>
.hero {
  position: relative;
  height: 650px;
  overflow: hidden;
  margin-top: 110px; /* adjust this to match your nav height */
  margin-bottom: 20px; /* add this to reduce the gap */
}

  .bg-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 650px;
    object-fit: cover;
    z-index: 0;
  }

  .hero-text {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: black;
    z-index: 2;
    font-family: 'Roboto', sans-serif;
    font-size: 20px;
  }

  .nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  padding:5px 40px; /* your current padding */
  background-color: rgba(13, 2, 126, 0.75); /* your transparency */
  color: white;
  display: flex;
  justify-content: space-between;
  align-items: center;
  z-index: 3;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-family: 'Roboto Serif', serif;
  font-weight: 200; /* or 200 for extra light */
  font-size: 42px; /* start here and adjust */
  white-space: nowrap;
 }

.logo img {
  height: 100px; /* adjust this to fit nicely */
  width: auto;
}

  .menu {
     display: flex;
  gap: 1rem;
  margin-right: 120px; /* pushes menu left from right edge */
  }

  .menu a {
  font-family: 'Roboto Serif', serif;
  font-weight: 200; /* or 200 for extra light */
  font-size: 20px; /* start here and adjust */
  color: white;
  text-decoration: none;
  }
</style>
</section>
