---
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alura Visuals - High-End Visual Content</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Times New Roman', serif;
            line-height: 1.6;
            color: #333;
            background: #ffffff;
        }

        /* Header - Exact Match */
        .header {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 30px 60px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 24px;
            font-weight: 300;
            color: #333;
            text-decoration: none;
            letter-spacing: 2px;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 40px;
            align-items: center;
        }

        .nav-menu a {
            text-decoration: none;
            color: #333;
            font-size: 16px;
            font-weight: 300;
            transition: opacity 0.3s ease;
        }

        .nav-menu a:hover {
            opacity: 0.7;
        }

        .book-btn {
            background: #333;
            color: white !important;
            padding: 12px 30px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 400;
        }

        .book-btn:hover {
            opacity: 0.8;
        }

        /* Hero Section - Exact Match */
        .hero {
            height: 100vh;
            background: #f8f6f4;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .hero-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .hero-video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.3;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
            max-width: 1000px;
            padding: 0 60px;
        }

        .hero-title {
            font-size: clamp(48px, 6vw, 80px);
            font-weight: 400;
            color: #333;
            line-height: 1.2;
            margin-bottom: 0;
            font-family: 'Times New Roman', serif;
        }

        /* Gallery Section - Exact Match */
        .gallery-section {
            padding: 0;
            background: #ffffff;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0;
            height: 100vh;
        }

        .gallery-item {
            position: relative;
            overflow: hidden;
            background: #f8f6f4;
        }

        .gallery-item img,
        .gallery-item video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .gallery-item:hover img,
        .gallery-item:hover video {
            transform: scale(1.05);
        }

        .gallery-arrows {
            position: absolute;
            bottom: 30px;
            right: 30px;
            display: flex;
            gap: 10px;
            z-index: 10;
        }

        .arrow-btn {
            width: 50px;
            height: 50px;
            background: rgba(0,0,0,0.7);
            border: none;
            border-radius: 50%;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .arrow-btn:hover {
            background: rgba(0,0,0,0.9);
        }

        /* Content Section - Your Conversion Message */
        .content-section {
            padding: 120px 60px;
            background: #ffffff;
            max-width: 1200px;
            margin: 0 auto;
        }

        .content-title {
            font-size: clamp(36px, 4vw, 60px);
            font-weight: 400;
            color: #333;
            margin-bottom: 40px;
            font-family: 'Times New Roman', serif;
            line-height: 1.3;
        }

        .content-description {
            font-size: 18px;
            color: #666;
            line-height: 1.8;
            max-width: 800px;
            font-weight: 300;
        }

        .content-images {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-top: 80px;
        }

        .content-image {
            border-radius: 20px;
            overflow: hidden;
        }

        .content-image img,
        .content-image video {
            width: 100%;
            height: 400px;
            object-fit: cover;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header {
                padding: 20px 30px;
                flex-direction: column;
                gap: 20px;
                background: rgba(255,255,255,0.95);
            }

            .nav-menu {
                gap: 20px;
            }

            .hero-content {
                padding: 0 30px;
            }

            .gallery-grid {
                grid-template-columns: 1fr;
                height: auto;
            }

            .gallery-item {
                height: 50vh;
            }

            .content-section {
                padding: 60px 30px;
            }

            .content-images {
                grid-template-columns: 1fr;
                gap: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Header - Exact Match -->
    <header class="header">
        <a href="#" class="logo">Alura Visuals</a>
        <nav>
            <ul class="nav-menu">
                <li><a href="#services">Services</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
                <li><a href="#" class="book-btn">Book now</a></li>
            </ul>
        </nav>
    </header>

    <!-- Hero Section with Your Video -->
    <section class="hero">
        <div class="hero-background">
            <!-- Replace with your Veo3 jewelry video -->
            <video class="hero-video" autoplay muted loop>
                <source src="YOUR_JEWELRY_VIDEO.mp4" type="video/mp4">
            </video>
        </div>
        <div class="hero-content">
            <h1 class="hero-title">Get high-end visuals that elevate your brand — free for first-time clients.</h1>
        </div>
    </section>

    <!-- Gallery Section - Like His Images -->
    <section class="gallery-section">
        <div class="gallery-grid">
            <div class="gallery-item">
                <!-- Replace with your generated content -->
                <img src="https://images.unsplash.com/photo-1515562141207-7a88fb7ce338?w=800&h=600&fit=crop" alt="Jewelry 1">
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1506630448388-4e683c67ddb0?w=800&h=600&fit=crop" alt="Jewelry 2">
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1515562141207-7a88fb7ce338?w=800&h=600&fit=crop" alt="Jewelry 3">
                <div class="gallery-arrows">
                    <button class="arrow-btn">←</button>
                    <button class="arrow-btn">→</button>
                </div>
            </div>
        </div>
    </section>

    <!-- Content Section - Your Conversion Message -->
    <section class="content-section">
        <h2 class="content-title">Make Your Designs Stand Out</h2>
        <p class="content-description">
            These custom visuals were designed to match each brand's style and sell their products with clarity. But here's the difference: while pretty images get attention, <strong>converting visuals get results</strong>. Let's create something that doesn't just look good — it drives action and builds your business.
        </p>
        
        <div class="content-images">
            <div class="content-image">
                <!-- Replace with your content -->
                <img src="https://images.unsplash.com/photo-1515562141207-7a88fb7ce338?w=600&h=400&fit=crop" alt="Converting Visual 1">
            </div>
            <div class="content-image">
                <img src="https://images.unsplash.com/photo-1506630448388-4e683c67ddb0?w=600&h=400&fit=crop" alt="Converting Visual 2">
            </div>
        </div>
    </section>
</body>
</html>
