---
---
export async function getStaticPaths() {
  // Try different glob patterns to find the file
  try {
    const videos = await Astro.glob('../../content/videos/*.md');
    console.log('Found videos:', videos.length);
    
    return videos.map((video) => ({
      params: { slug: video.frontmatter.slug },
      props: { video },
    }));
  } catch (error) {
    console.error('Glob error:', error);
    return [];
  }
}

const { video } = Astro.props;
const { Content } = video;
const frontmatter = video.frontmatter;
---
---

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{frontmatter.title} | Natural Farm Secrets</title>
  <meta name="description" content={frontmatter.description} />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto+Serif:wght@200;300;400&family=Nunito+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <!-- Breadcrumbs -->
  <div class="breadcrumbs">
    {frontmatter.breadcrumbs.map((crumb, index) => (
      <span>
        {index < frontmatter.breadcrumbs.length - 1 ? (
          <>
            <a href="/">{crumb}</a>
            <span class="separator">›</span>
          </>
        ) : (
          <span class="current">{crumb}</span>
        )}
      </span>
    ))}
  </div>

  <!-- Navigation -->
  <header class="nav">
    <div class="logo">
      <img src="/logonoback250.png" alt="Natural Farm Secrets" />
      <span>Natural Farm Secrets</span>
    </div>
    <nav class="menu">
      <a href="/">Home</a>
      <a href="/#teachers">Teachers</a>
      <a href="/contact">Contact</a>
    </nav>
  </header>

  <!-- Article Content -->
  <main class="article-container">
    <div class="article-content">
      <div class="headline-section">
        <span class="category-tag">{frontmatter.category}</span>
        <h1 class="headline">{frontmatter.title}</h1>
        <h2 class="subheadline">{frontmatter.subtitle}</h2>
        <div class="byline">
          <span>By {frontmatter.author}</span>
          <time class="date">{frontmatter.publishDate}</time>
        </div>
      </div>

      <div class="featured-media">
        <iframe 
          width="100%" 
          height="400" 
          src={`https://www.youtube.com/embed/${frontmatter.videoId}`}
          title={frontmatter.title}
          frameborder="0" 
          allowfullscreen>
        </iframe>
        <p class="media-caption">Tony Busatta shares the story of how earthworms transformed his struggling vineyard</p>
      </div>

      <div class="story-content">
        <Content />
      </div>
    </div>

    <aside class="sidebar">
      <div class="sidebar-section">
        <h4>Tony's Video Series</h4>
        <div class="sidebar-video">
          <div class="video-thumb">🎬</div>
          <p>Episode 1: The Origin Story</p>
          <small>You are here</small>
        </div>
      </div>
    </aside>
  </main>
</body>
</html>

<style>
/* Copy all your beautiful CSS from the previous template here */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Nunito Sans', sans-serif;
  line-height: 1.6;
  color: #000;
  background-color: #f8f9fa;
}

.breadcrumbs {
  background: #f8f9fa;
  padding: 10px 40px;
  margin-top: 80px;
  font-size: 14px;
  border-bottom: 1px solid #eee;
}

.breadcrumbs a {
  color: #16008A;
  text-decoration: none;
}

.separator {
  margin: 0 8px;
  color: #999;
}

.current {
  color: #666;
  font-weight: 600;
}

.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  padding: 20px 40px;
  background-color: rgba(22, 0, 138, 0.9);
  color: white;
  display: flex;
  justify-content: space-between;
  align-items: center;
  z-index: 100;
  height: 80px;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-family: 'Roboto Serif', serif;
  font-weight: 300;
  font-size: 24px;
}

.logo img {
  height: 50px;
  width: auto;
}

.menu {
  display: flex;
  gap: 2rem;
  margin-right: 40px;
}

.menu a {
  font-family: 'Roboto Serif', serif;
  font-weight: 300;
  font-size: 18px;
  color: white;
  text-decoration: none;
}

.article-container {
  display: grid;
  grid-template-columns: 2.5fr 0.5fr;
  gap: 20px;
  max-width: 1200px;
  margin: -125px auto 0;
  padding: 40px;
}

.article-content {
  background: white;
  padding: 15px 25px 25px 25px;
  border-radius: 8px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
}

.headline-section {
  margin-bottom: 30px;
}

.category-tag {
  background: #16008A;
  color: white;
  padding: 6px 16px;
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 15px;
  display: inline-block;
  border-radius: 4px;
}

.headline {
  font-family: 'Roboto Serif', serif;
  font-weight: 300;
  font-size: 42px;
  line-height: 1.0;
  margin-bottom: 5px;
  color: #000;
}

.subheadline {
  font-family: 'Roboto Serif', serif;
  font-weight: 200;
  font-size: 24px;
  color: #666;
  margin: 0 0 20px 0;
  font-style: italic;
}

.byline {
  display: flex;
  gap: 20px;
  font-size: 14px;
  color: #999;
  border-bottom: 1px solid #eee;
  padding-bottom: 20px;
  margin-bottom: 30px;
}

.featured-media {
  margin: 15px 0 30px 0;
}

.featured-media iframe {
  width: 100%;
  height: 400px;
  border-radius: 8px;
}

.media-caption {
  font-size: 14px;
  color: #666;
  font-style: italic;
  margin-top: 10px;
  text-align: center;
}

.story-content {
  font-size: 16px;
  line-height: 1.8;
  margin-bottom: 40px;
}

.story-content p {
  margin-bottom: 24px;
}

.story-content h2 {
  font-family: 'Roboto Serif', serif;
  font-size: 28px;
  margin: 40px 0 20px 0;
  color: #16008A;
}

.sidebar {
  display: flex;
  flex-direction: column;
  gap: 30px;
}

.sidebar-section {
  background: white;
  padding: 15px;
  border-radius: 8px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
}

.sidebar h4 {
  font-family: 'Roboto Serif', serif;
  font-size: 20px;
  margin-bottom: 20px;
  color: #16008A;
}

.sidebar-video {
  margin-bottom: 15px;
}

.video-thumb {
  width: 100%;
  height: 60px;
  background: #ddd;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 8px;
  border-radius: 4px;
  font-size: 20px;
}
</style>